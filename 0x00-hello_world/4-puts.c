#include <stdio.h>
/**
 * main - Contains all relevant blocks of code
 * Return: Must always return a value of 0
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
